# Rate Limiter Service
A REST API that enforces per-client request limits using a token bucket
algorithm. Built with `FastAPI`, backed by `Redis`, with a benchmark suite
measuring allow/block ratios under load.

# Tech Stack
 - Python
 - FastAPI
 - Redis
 - Pytest





